# Optional — only needed for the REAL frozen-LLM planner (planner.backend: openrouter).
# Without these, the offline "mock" backend is used and the benchmark still runs.
OPENROUTER_API_KEY=sk-or-v1-...
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
PLANNER_MODEL=google/gemma-4-31b-it:free
